{
  "name": "nestjs-discord-webhook-logger",
  "version": "2.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "author": {
    "name": "Ly"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "vitest",
    "build": "rimraf dist && tsc -p tsconfig.build.json",
    "prepare": "npm run build",
    "release": "npm run build && release-it"
  },
  "dependencies": {
    "@discordjs/builders": "^1.0.0",
    "axios": "^0.27.2"
  },
  "devDependencies": {
    "@nestjs/common": "^9.0.5",
    "@nestjs/core": "^9.0.5",
    "reflect-metadata": "^0.1.13",
    "release-it": "^15.1.3",
    "rimraf": "^3.0.2",
    "rxjs": "^7.5.6",
    "typescript": "^4.7.4",
    "vitest": "^0.18.1"
  }
}
